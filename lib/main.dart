import 'package:flutter/material.dart';
import 'src/app.dart'; //import M (Gracias a la extensión Awesome Flutter Snipets)
void main() => runApp(new Contador()); //poner la función de flecha es lo mismo que meter runApp en corchetes